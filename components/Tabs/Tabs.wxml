<view class="tab">
	<view class="tab_item {{item.checked ? 'tab_item_checked':''}}" wx:for="{{tabs}}" wx:key="tab_id" bindtap="tapEvent" data-operation="{{index}}">
		{{item.tab_name}}
	</view>
</view>
<view class="tab_content">
<slot></slot>
</view>
